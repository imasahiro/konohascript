/* Math */
function __Math__() {
}
__Math__.sqrt = function(f) {
    return Math.sqrt(f);
}
__Math__.cos = function(f) {
    return Math.cos(f);
}
__Math__.sin = function(f) {
    return Math.sin(f);
}
__Math__.floor = function(f) {
    return Math.floor(f);
}
