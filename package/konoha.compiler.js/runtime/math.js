/* Math */
function Math_() {
}
Math_.sqrt = function(f) {
    return Math.sqrt(f);
}
Math_.cos = function(f) {
    return Math.cos(f);
}
Math_.sin = function(f) {
    return Math.sin(f);
}
Math_.floor = function(f) {
    return Math.floor(f);
}
